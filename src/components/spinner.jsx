import React from "react";
import './spinner.css'; // Assuming you have a spinner.css file for styling

const Spinner = () => {
  return (
    <span className="loader"></span>
  );
}

export default Spinner;